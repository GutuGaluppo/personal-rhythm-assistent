{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pause",
  "description": "Pause window: it can read the pause, start it, and end it. Nothing else.",
  "windows": ["pause"],
  "permissions": ["allow-get-pause-view", "allow-start-pause", "allow-end-pause"]
}
